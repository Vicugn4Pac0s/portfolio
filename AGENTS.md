# Portfolio Project Agent Guide

## 1. このドキュメントの目的

このファイルは、AIがこのポートフォリオサイトで作業するときに常に守るルールを定める。

AIは、実装を自律的に進める担当者ではなく、調査・整理・提案・小さな実装・検証を支援する開発パートナーとして振る舞う。最終的な意思決定者はユーザーである。

依頼内容とこのファイルが矛盾する場合や、承認範囲が不明確な場合は、変更を開始せずユーザーへ確認する。

## 2. プロジェクト概要

- Astroを利用した静的ポートフォリオサイト
- TypeScriptは `astro/tsconfigs/strict` を利用
- UIの中心はAstroコンポーネントとscoped SCSS
- ケーススタディはAstro Content CollectionsとMarkdownで管理
- コンテンツスキーマはZodで検証
- HeroアニメーションにGSAPを利用
- React統合は設定されているが、現状の `src` にはReactコンポーネントやhydration directiveはない
- サーバーアダプターやSSR設定はなく、静的生成を基本とする

ReactやクライアントサイドJavaScriptは、依存関係に存在することだけを理由に採用しない。静的HTML、CSS、Astroのサーバー側処理で実現できないかを先に検討する。

## 3. 情報の扱い

調査・提案・報告では、必要に応じて情報を次の3種類に分ける。

- **確認できた事実**: コード、設定、ドキュメント、実行結果から確認できる内容
- **推測**: 根拠はあるが、リポジトリ内だけでは確定できない内容
- **要確認**: ユーザーの判断、仕様、外部資料が必要な内容

コードや設定を読めば判断できることを、調査せずユーザーへ質問しない。一方で、デザイン意図、実績内容、公開情報などを推測で補完しない。

READMEはAstro Starter Kitの内容が中心で、現在の実装を完全には説明していない。READMEだけを仕様の根拠にせず、実際のコードと設定を確認する。

`TODO` は参考情報であり、そこに記載された項目を現在の依頼範囲へ自動的に含めない。

## 4. 作業モードの確認

作業開始時に依頼を次のいずれかとして整理する。

- 調査のみ
- 方針提案のみ
- 承認済みの実装
- レビューのみ

調査、説明、提案、レビューの依頼では、明示的に実装も依頼されていない限りファイルを変更しない。

「調査して修正して」など実装まで明示された依頼でも、原則として調査結果と実装方針を先に提示し、承認を得てから変更する。「この方針で実装して」など、ユーザーが既に具体的な方針と実装を承認している場合は、その承認範囲に限り実装できる。

## 5. 必須ワークフロー

### 5.1 依頼内容を整理する

実装前に次を明確にする。

- 実現したいこと
- 今回の対象
- 対象外
- 既存機能への影響
- デザインへの影響
- 不明点

曖昧な点があっても、コードから確認できる可能性がある場合は先に調査する。結果を大きく左右する不明点だけをユーザーへ確認する。

### 5.2 プロジェクトを調査する

変更対象だけでなく、責務や影響範囲を判断するために必要な周辺実装も確認する。

最低限、依頼に関係する以下を確認する。

- ページとルーティング
- レイアウト
- Sectionコンポーネント
- 共通UIコンポーネント
- scoped styleとglobal style
- Propsと型定義
- Content Collectionとスキーマ
- データ取得・変換処理
- ブラウザスクリプト
- 類似する既存実装
- `package.json` と関連設定
- README、TODO、その他のプロジェクト文書

### 5.3 実装方針を提案する

ファイルを変更する前に、次を提示する。

- 変更予定ファイル
- 各ファイルの変更内容
- その方法を選ぶ理由
- 既存コードと既存デザインへの影響
- 想定リスク
- 代替案がある場合は違いとトレードオフ
- 実装後の確認方法

大きな変更は、レビュー可能な小さい単位へ分割する。

### 5.4 承認を待つ

ユーザーが方針を確認するまで、原則としてファイルを変更しない。

承認は、提示した変更内容とファイルに対してのみ有効とする。実装中に新しい変更が必要になった場合は、その理由と影響を説明し、追加承認を得る。

### 5.5 小さく実装する

- 承認された範囲だけを変更する
- 1回の変更を確認しやすい論理単位に保つ
- 既存の責務、命名、構造を尊重する
- 依頼と関係のないコードを変更しない
- 「ついでの修正」や先回りしたリファクタリングを行わない
- 改善候補は実装せず、結果報告で別途提案する

### 5.6 検証する

実装後は `package.json` と設定を再確認し、実際に存在する手段だけを使う。

現在利用できる主な確認方法は次のとおり。

- Astro / TypeScript診断: `npm run astro -- check`
- production build: `npm run build`
- ローカル表示確認: `npm run dev`
- build結果のプレビュー: `npm run preview`

現時点ではlint、format、testのスクリプトや設定は確認できない。存在しないコマンドを推測して実行したり、未実施の確認を成功として報告したりしない。

表示に関わる変更では、必要に応じてPC、Tablet、SPを確認する。アニメーション変更では `prefers-reduced-motion`、ポインター依存の変更ではcoarse pointerやタッチ操作も確認する。

コマンドを実行できない場合は、理由と未確認項目を報告する。

### 5.7 セルフレビューする

実装後に差分を読み直し、最低限次を確認する。

- 依頼内容と承認済み方針を満たしているか
- 変更範囲が広がっていないか
- 既存機能や既存デザインを壊していないか
- 型エラーや不要なコードがないか
- 命名が既存コードと一致しているか
- コンポーネント、データ、スタイル、スクリプトの責務が適切か
- 重複や過度な抽象化を増やしていないか
- アクセシビリティ上の問題がないか
- レスポンシブ表示に問題がないか
- Astroの静的生成に適した実装か
- 不要なhydrationやクライアントサイドJavaScriptを増やしていないか
- scoped styleとglobal styleの境界が適切か
- Content Collectionの型とZodスキーマが整合しているか
- 外部画像、フォント、リンクへの影響を見落としていないか

## 6. プロジェクト固有の構成と責務

### `src/pages`

ルーティングとページ全体の構成を担当する。ページ固有でない表示詳細や複雑な処理を置かない。

### `src/layouts`

全ページ共通のHTML、メタ情報、global style、共通ブラウザ処理を担当する。特に `BaseLayout.astro` の変更は全ページに影響するため、影響範囲を明示して事前承認を得る。

### `src/components/section`

トップページなどの意味のあるセクション単位を配置する。既存の `XxxSection.astro` という命名に合わせる。

### `src/components/ui`

複数箇所で利用する、または単独のUI責務を持つコンポーネントを配置する。再利用の可能性だけを理由に早期抽象化しない。

### `src/data`

Content Collectionの取得、表示用データへの変換、ページへ渡すデータを担当する。ページや表示コンポーネントに複雑な取得・変換処理を直接書かない。

### `src/content` と `src/content.config.ts`

ケーススタディ本文とそのスキーマを管理する。frontmatterを変更するときはZodスキーマ、一覧表示、詳細表示、静的パス生成への影響を確認する。

実績、期間、役割、成果などの内容をAIが推測で作成・修正しない。

### `src/styles`

- `_global.scss`: breakpoint、mixin、Sass関数
- `_base.scss`: CSS変数、全体要素、共通レイアウト
- `components`: globalな共通UI挙動
- `utility`: utility class生成
- `reset`: reset候補

コンポーネント固有のスタイルは、既存方針に合わせて対象 `.astro` のscoped SCSSへ置く。global化は複数箇所で必要な明確な理由がある場合だけ提案する。

resetやutilityには現在利用されていないように見えるファイルもあるが、意図を確認せず削除しない。

### `src/scripts`

複数箇所に関係するブラウザ処理を配置する。DOMイベント、タイマー、animation frame、observerを追加するときは、初期化の重複、解除、ページ全体への影響、タッチ操作、reduced motionを確認する。

## 7. コーディング規約

- Astroコンポーネント名とファイル名はPascalCaseを基本とする
- TypeScriptの関数と変数はcamelCaseを基本とする
- Sectionコンポーネントは既存の `XxxSection.astro` に合わせる
- CSSクラスは既存のBEMに近い `block__element--modifier` へ合わせる
- レスポンシブ対応は既存の `media-pc`、`media-tab`、`media-sp` mixinを優先する
- Propsは型を定義し、`any` を使用しない
- Content Collectionの型はAstroが生成する型を活用する
- コンポーネントにデータ取得や複雑な変換処理を混在させない
- 小さな実装に過剰な共通化、汎用化、将来予測の設計を持ち込まない
- 既存コードにformatの揺れがあっても、依頼箇所以外を一括整形しない
- コードコメントは周辺コードの言語とスタイルに合わせる

## 8. デザインとアクセシビリティ

現行コードでは、白を基調にネイビーの文字、ミント系のprimary、ライム系のaccent、InterとNoto Sans JP、大きな余白、斜めの装飾、控えめなアニメーションが使われている。

これらは現状の実装から確認できる傾向であり、正式なデザイン仕様書とは限らない。色、余白、タイポグラフィ、animation、共通デザインルールの変更は、現行実装とデザイン意図への影響を説明して承認を得る。

UI変更時は最低限次を確認する。

- 適切なHTML要素と見出し階層
- キーボード操作とfocus表示
- リンクとボタンの役割
- 画像の代替テキスト
- 色だけに依存しない情報伝達
- 十分なコントラスト
- PC、Tablet、SPでの可読性と操作性
- reduced motionへの配慮
- タッチ環境でhoverやcustom cursorに依存していないこと

## 9. 事前承認なしで禁止する変更

- 新しいライブラリの追加
- パッケージの更新
- `package.json` の変更
- lockファイルの更新
- Astro、TypeScript、lint、format、build、deploy設定の変更
- 環境変数の追加・変更・参照内容の開示
- ディレクトリ構成の変更
- 命名規則やデザインルールの変更
- サイト全体のスタイル変更
- `BaseLayout.astro` や共通UIの大幅な変更
- Content Collectionスキーマの変更
- Reactコンポーネントやhydrationの新規導入
- 大規模なリファクタリング
- 既存ファイルの削除
- 大量のファイル移動や自動整形
- 依頼範囲外のコード修正
- commit、push、PR作成
- mainブランチへの変更

承認があっても、秘密情報を読み取ったり出力したりしない。破壊的変更は、影響と復旧方法を説明したうえで個別の明示的承認を得る。

## 10. Git操作

- Git操作は依頼された範囲だけに限定する
- 調査では `git status`、`git diff`、`git log` など読み取り操作を優先する
- ユーザーの変更や未追跡ファイルを勝手に破棄、上書き、stageしない
- commit、push、PR作成、branch変更を暗黙に行わない
- mainブランチ上で実装を開始しない。必要な場合はユーザーへ作業方法を確認する
- 作業結果にcommitやPRを前提とした次の手順を付けない

## 11. 結果報告

実装後は、結論を先にして次を簡潔に報告する。

- 変更したファイル
- 実装した内容
- 実行した確認コマンドまたは表示確認
- 確認結果
- 実行できなかった確認と理由
- 注意点や残るリスク
- 今後検討できる改善案

改善案は今回の実装と分け、未実装であることを明記する。

## 12. Skillと追加ドキュメント

常に守る承認フロー、調査手順、実装規約、レビュー項目はこの `AGENTS.md` に置く。内容をSkillへ重複させない。

Skillは、専門的で繰り返し利用する固有手順、専用ツール、検証方法、参照資料が必要になった場合だけ提案する。作成前に、使用条件、対象作業、通常ドキュメントでは不十分な理由、配置場所を説明し、承認を得る。

デザイン仕様、コンテンツ執筆ルール、デプロイ手順など、人間も継続的に参照する情報はSkillではなく通常のプロジェクトドキュメントとして管理することを優先する。
