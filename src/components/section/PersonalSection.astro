---
import SectionHeading from '../ui/SectionHeading.astro';
---

<section id="personal" class="personal section-block">
  <div class="site-container">
    <SectionHeading>PERSONAL</SectionHeading>
    <div class="personal-panel">
      <figure class="personal-panel__media">
        <img
          class="personal-panel__image"
          src="/images/personal-img.webp"
          alt="Yoshiki Yamada"
          loading="lazy"
        />
      </figure>
      <div class="personal-panel__text">
        <h2 class="section-title">
          仕事でもプライベートでも、積み重ねることが好きです。
        </h2>
        <div class="personal-panel__body">
          <p>ランニングや筋トレを継続し、フルマラソンにも挑戦。休日はカフェで個人開発や技術の学習に取り組み、自分のペースで成長を楽しんでいます。</p>
          <p>仕事でも同じように、一つひとつ改善を積み重ねながら、より良いものづくりを目指しています。</p>
        </div>
      </div>
    </div>
  </div>
</section>

<style lang="scss">
  @use '@styles/global' as *;

  .personal-panel {
    --image-size: 380px;
    @include media-tab {
      --image-size: 280px;
    }
    
    display: grid;
    grid-template-columns: minmax(0, var(--image-size)) minmax(0, 1fr);
    gap: 4.8rem;
    align-items: center;

    @include media-tab {
      gap: 3.2rem;
    }

    @include media-sp {
      grid-template-columns: 1fr;
      gap: 3.2rem;
    }

    &__media {
      margin: 0;
      overflow: hidden;
    }

    &__image {
      display: block;
      width: 100%;
      height: 100%;
      min-height: var(--image-size);
      object-fit: cover;

      @include media-sp {
        min-height: 280px;
      }
    }

    &__text {
      min-width: 0;
    }

    &__body {
      display: grid;
      gap: 1.6rem;
      color: var(--color-muted);
      font-size: 1.6rem;
      line-height: 2;

      @include media-sp {
        font-size: 1.4rem;
        line-height: 1.8;
        gap: 1.2rem;
      }
    }
  }
</style>
